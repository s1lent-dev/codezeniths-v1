model Module {
  id          String      @id @default(uuid(7)) @db.Uuid
  title       String
  slug        String      @unique
  description String?
  topics      Topic[]
  tags        Tag[]
  skills      Skill[]
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  @@map("module")
}
